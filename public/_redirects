
# Netlify redirects file with improved crawler detection

# Static files - serve directly first
/sitemap.xml /sitemap.xml 200
/robots.txt /robots.txt 200
/favicon.ico /favicon.ico 200
*.png /lovable-uploads/:splat 200
*.jpg /lovable-uploads/:splat 200
*.jpeg /lovable-uploads/:splat 200
*.svg /lovable-uploads/:splat 200

# API routes (if any)
/api/* /api/:splat 200

# Route ALL page requests through our page-router function for proper crawler detection
# This handles both crawlers (gets SEO content) and humans (gets redirected to React app)
/* https://yxsnoncplnzekfwaknxb.supabase.co/functions/v1/page-router 200
